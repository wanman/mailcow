# mysql_virtual_sender_acl.cf
user = my_mailcowuser
password = my_mailcowpass
hosts = my_dbhost
dbname = my_mailcowdb
query = SELECT alias.goto, sender_acl.logged_in_as FROM alias LEFT JOIN sender_acl ON sender_acl.send_as=alias.address WHERE alias.address='%s'
